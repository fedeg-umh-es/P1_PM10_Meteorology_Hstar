# 01_scope_protocol.md

Pendiente de definir el alcance bloqueado de P1.

Decisión ya tomada:
- P1 vive en un repositorio separado del estudio Madrid-Valencia.

Base metodológica heredada:
- rolling-origin estricto
- preprocesado train-only
- persistencia como baseline explícita
- Skill(h) vs persistencia
- H*(relax) y H*(strict)

Pendiente de fijar para P1:
- dominio geográfico exacto
- estaciones incluidas
- variables meteorológicas/exógenas
- comparación `lag-only` vs `lag+meteo`
- lista final de modelos permitidos
